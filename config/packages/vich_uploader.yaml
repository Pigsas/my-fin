vich_uploader:
    db_driver: orm

    metadata:
        type: attribute

    mappings:
        expenses:
            uri_prefix: /files/expenses
            upload_destination: '%kernel.project_dir%/var/files/expenses'
            namer: Vich\UploaderBundle\Naming\SmartUniqueNamer
            inject_on_load: true
